Validation means =>  Never trust data coming from client
user can send unnecessary/unsafe data

now: 
Request → Controller → Service → Database


after validation:
Request → Validation Middleware → Controller → Service → Database


first we learn => custom validator with js
after that => joi


src/
├── validators/
│   └── authValidator.js


//
custom validator => 
rules(schema) create => validation => middleware => use before controller

# has maintaining and expending issue when project grow
# a lot of work
# array validation => more code
# nested obj => much more code


libraries save time, prevent bug and reduce complexity 



// joi npm

joi simplify the validation

=> create schema with joi => 


// joi already know how to validate
// we don't need to validate manually like custom validator
// if (!fullname) ...
// if (fullname.length < 3) ...
// if (!email.includes("@")) ...

// so joi automatically validate data. and schema just a set of rules



// # Error message in terminal =>Nodejs show it in a special way



//
// joi error just send message. no status code
// that's why joi Error => convert => AppError

// joi:
// # Stop validation after the first error. that's why we can see just one error message
// abortEarly: true

// we can change it to => abortEarly: false, means
// Don't stop at the first error.
// Collect all errors.


//
// production APIs:
// for return Error message => 3 options
=> just one error => easier - joi default

=> one long error(multiple message) => work, but hard for frontend developer
=> Array of Error - industry standard => easier for react/frontend developer


real APIs usually return validation errors => errors array

# because, react form => show the error under each field
# we can simply use array error. but giant string need to parse

